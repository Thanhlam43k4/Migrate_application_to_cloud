import nodemailer from 'nodemailer'
import * as dotenv from 'dotenv'
dotenv.config({ path: '../.env' });
import verificationLink from './customer.js'

export const sendEmailService = async (email) => {
    return info;
}